<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @short_url.user_id %>
</p>

<p>
  <strong>Url:</strong>
  <%= @short_url.url %>
</p>

<%= link_to 'Edit', edit_short_url_path(@short_url) %> |
<%= link_to 'Back', short_urls_path %>

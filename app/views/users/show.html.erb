<div class="col-md-6 col-md-offset-3">
  <div class="show-user">
    <h3><%= @user.name  if @user.name %></h3>
    <%= @user.email if @user.email %>
  </div>
  <%= render 'shared/short_urls' %>
</div>
